<h1>Categories Index</h1>
<ol>
<% @categories.each do |category| %>
  <li><%= category.name %> <%= link_to "Delete", admin_category_path(category), method: :delete %></li>
<% end %>
</ol>

<%= link_to "Add Category", new_admin_category_path %>
